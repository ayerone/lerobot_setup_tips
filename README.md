# lerobot_setup_tips
Stumbles and hints for setting up LeRobot

**consistent_usb_serial_ports** a method of finding consistent names for the serial ports that don't change between boots or when plugging & re-plugging

**find_camera_indeces** if you have multiple cameras, the indeces tend to change between boots; a short script for quicky sorting them out
